# AstroLLM Python Dependencies
# Prefer: uv sync (uses pyproject.toml)
# Fallback: uv pip install -r requirements.txt

# ── Core ML ──
torch>=2.4.0
transformers>=4.45.0
peft>=0.13.0
trl>=0.12.0
accelerate>=1.0.0
bitsandbytes>=0.44.0
datasets>=3.0.0
tokenizers>=0.20.0

# ── Efficient Training ──
unsloth[colab-new]>=2024.11
flash-attn>=2.6.0
xformers>=0.0.28

# ── Experiment Tracking ──
wandb>=0.18.0

# ── Model Merging ──
mergekit>=0.0.5

# ── Quantization & Serving ──
# llama-cpp-python>=0.3.0  # Install separately with CUDA support
auto-gptq>=0.7.0
autoawq>=0.2.0

# ── Data Pipeline ──
arxiv>=2.1.0
pdfminer.six>=20231228
beautifulsoup4>=4.12.0
lxml>=5.0.0
ftfy>=6.2.0
langdetect>=1.0.9
datasketch>=1.6.0  # MinHash for deduplication

# ── Astronomy Tools ──
astropy>=6.0.0
astroquery>=0.4.7  # SIMBAD, VizieR, MAST, NED
ads>=0.12.6  # NASA ADS

# ── RAG ──
sentence-transformers>=3.0.0
pgvector>=0.3.0
psycopg2-binary>=2.9.0

# ── Evaluation ──
lm-eval>=0.4.0  # EleutherAI evaluation harness
rouge-score>=0.1.2
sacrebleu>=2.4.0

# ── Utilities ──
pyyaml>=6.0
python-dotenv>=1.0.0
tqdm>=4.66.0
numpy>=1.26.0
pandas>=2.2.0
jsonlines>=4.0.0
rich>=13.7.0
typer>=0.12.0
httpx>=0.27.0
